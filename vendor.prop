# Bluetooth
bluetooth.device.default_name=moto g53j 5G

# Camera
persist.vendor.camera.physical.num=3

# Motorola
ro.mot.build.customerid=global

# NFC
ro.vendor.hw.nfc=ese_nq
ro.hardware.nfc_nci=pn54x
persist.nfc_cfg.config_file_name=libnfc-nci_SN100.conf

# SOC
ro.soc.model=SM4350

# Surfaceflinger
ro.surface_flinger.has_HDR_display=false
ro.surface_flinger.has_wide_color_display=false

# Radio
persist.radio.multisim.config=dsds
